apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oracle-container-repositories-svc.fullname" . }}-test-connection"
  labels:
    {{- include "oracle-container-repositories-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oracle-container-repositories-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
